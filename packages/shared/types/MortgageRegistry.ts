export type MortgageRegistry = {
  // eslint-disable-next-line @typescript-eslint/naming-convention
  _id: string;
  address: string;
  cities: Array<string>;
  districtName: string;
  districtNumber: string;
  email: string;
  moreInfos: string;
  phone: string;
  title: string;
};
